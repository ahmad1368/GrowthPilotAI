/// A merchant's trust tier band (Issue #347, acceptance criterion 3) —
/// `gold` merchants automatically receive the commission discount
/// privilege.
enum MerchantTrustTier { restricted, standard, gold }
