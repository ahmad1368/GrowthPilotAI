/// Who sent a `SupportMessageEntity` (Issue #193) — `agent` covers
/// both the local canned auto-reply and any future human reply; no
/// real support staff exist in this repo (see PR notes).
enum SupportMessageSender { user, agent }
