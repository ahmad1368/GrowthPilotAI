/// The kind of engagement telemetry event recorded for a merchant
/// (Issue #424, acceptance criterion 1) — both count toward daily
/// dashboard-visit frequency.
enum MerchantActivityEventType { login, dashboardVisit }
