/// "Page Toggling: a checkbox list to include/exclude specific pages
/// (e.g., 'Exclude Traceability Matrix from this version')" (Issue
/// #259) — which pages appear in the previewed/shared PDF report.
enum TraceabilityReportSection { summary, matrix, requirements }
