/// What action an ACTION_CARD message asks the recipient to take (Issue
/// #73) — mirrors the original issue's `actionType` field.
enum ActionCardType { approveTransaction, signContract, payInvoice }
