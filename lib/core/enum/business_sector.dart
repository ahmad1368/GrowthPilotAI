/// BC industry sectors the Business Compass (Issue #84) benchmarks against
/// (Issue #83's "Success Signature", client-side mocked here — see
/// [GetSectorBenchmark]).
enum BusinessSector { construction, tech, retail }
