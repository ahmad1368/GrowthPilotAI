/// A merchant's platform-dependency classification (Issue #424) —
/// `highDependency` merchants automatically unlock advanced billing
/// tiers and revenue-sharing eligibility.
enum MerchantDependencyTier { standard, engaged, highDependency }
