/// The Inbox's "Smart Category" buckets (Issue #77). [all] is the
/// pseudo-category for "no filter applied" — [ClassifyConversationCategory]
/// never returns it.
enum InboxCategory { all, financial, pending, support }
