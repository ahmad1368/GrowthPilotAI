/// Which narrative template [GenerateInsightNarrative] selected (Issue
/// #101 scope item 1: "Insight Template Engine") — at least 5 distinct
/// types plus a fallback, per the AC.
enum InsightNarrativeType {
  valueDiscovery,
  convenienceTradeOff,
  highDemandWarning,
  premiumPick,
  balancedMatch,
  marketCompetitive,
}
