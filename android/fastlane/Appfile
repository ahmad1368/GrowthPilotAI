# Issue #190 — real values come from CI secrets, never hardcoded.
# Package name still matches android/app/build.gradle.kts's current
# (placeholder-style) applicationId — update ANDROID_PACKAGE_NAME once
# this app has its real production id.
json_key_file(ENV["SUPPLY_JSON_KEY_PATH"])
package_name(ENV["ANDROID_PACKAGE_NAME"] || "com.example.growth_pilot_ai")
